#include <stdio.h>

int power(int a, int b) {
    int p = 1;
    for(int i=1; i<=b; i++)
        p *= a;
    return p;
}

int main() {
    int a, b;
    printf("Enter base and exponent: ");
    scanf("%d%d", &a, &b);
    printf("%d^%d = %d", a, b, power(a, b));
    return 0;
}
